package cwd;

public class Daemon_Thread {

	/*
			1. this is a dependent thread on other thread's life cycle.
			2. The best example of Daemon Thread is ---> << Garbage Collector >> Because
					it runs behind the program simultaneously and collects the garbage memory.

			3. Daemon threads are the service providers to another threads...
			4. The threads which are executing in the background are called daemon threads
			5. The main objective of daemon thread in Java is to provide support for non-daemon thread like main thread.

			6. Finalize-r and Garbage Collector is the real life example of the daemon thread.
  */
	boolean a =  true;
	// PRoducer -Consumer ALgorithm (Synchronization) , (inter-Thread Communications)


}
